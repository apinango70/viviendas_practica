<%= form_with(model: tipo_vivienda) do |form| %>
  <% if tipo_vivienda.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_vivienda.errors.count, "error") %> prohibited this tipo_vivienda from being saved:</h2>

      <ul>
        <% tipo_vivienda.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
